package lld.parking_lot.enums;

/*
 * Vehicle categories supported by this design.
 *
 * Adding a new type, such as ELECTRIC_CAR or TRUCK, would usually require a new
 * spot manager setup and possibly a new pricing rule.
 */
public enum VehicleType {
    MOTORCYCLE,
    CAR
}
